using UnityEngine;

[CreateAssetMenu(fileName = "Item", menuName = "ScriptableObjects/Throwable", order = 2)]
public class ThrowableSO : ItemSO
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
